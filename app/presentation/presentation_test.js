'use strict';

describe('myApp.presentation module', function() {

  beforeEach(module('myApp.presentation'));

  describe('Presentation controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var PresentationCtrl = $controller('PresentationCtrl');
      expect(PresentationCtrl).toBeDefined();
    }));

  });
});